

function setup() {
    createCanvas(400,400);
}

function draw() {
    background("#444");
    textSize(30);
    textAlign("center","center");
    noStroke();
    fill("white");
    text("dude what the sigma",200,200);
}